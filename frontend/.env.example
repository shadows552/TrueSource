# API Configuration
VITE_API_URL=http://localhost:3000/api

# Solana Network (devnet, testnet, mainnet-beta, or validator)
# Set to 'validator' to use local validator
VITE_SOLANA_NETWORK=devnet

# Local Solana Validator URL (used when VITE_SOLANA_NETWORK=validator)
# Required for local validator setup
VITE_SOLANA_VALIDATOR_URL=http://localhost:8899
